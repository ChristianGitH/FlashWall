TO DEPLOY :
composer.json : updated to laravel 13 and livewire 4. Not using Volt anymore, livewire does the same natively. Changes in routes and layout.
Images : removed field "permanent", not used anymore (old copies slideshow method), from DB and from all files.
AppServiceProvider.php : Now force main domaine and HTTPS for all generated URLs.
.htaccess : added redirection from www and http to main URL and https.


NEXT TO DO :
    CSRF on slideshow

moderation : refactor moderation components, DRY, uper_image_data, mayby also bulk_actions
    create a trait to not repete php functions, and maybe a child component for the buttons
setup-wall + display-wall : Video as background on wall display
display-image : Add possibility to display QR code on display-image
moderation : fix caption display, overlay bug with side menu drawer
languages : fix the problem due to two different maners of using languages, fr/terms for some pages and language/fr for switcher
moderation : ajouter un champ status_last_update pour eviter que les intéractions sur les légendes changent last_update

Espace insécables dans les légendes avant la ponctuation
Echap to close Image Preview in Moderation
End message style (not center now)
Epingler image dans moderation pour eviter actions
Allow possibility to add a frame around submitter image, and then download
Email for password reset and email confirmation
New moderation page for mobile, one iamge at a time, with swipe for example
Save submitter image at original format for user/sumbitter download



/*\ COMMAND LINES /*\
Reset BDD : 
php artisan migrate:fresh --seed

Start Vite server : npm run dev (or build)

Create volt component :
php artisan make:volt walls/display-images-dev --class



/*\ IMAGES COPIES LOGIC /*\
Si modération OFF :
Images ajoutées avec leurs copies, status = 1 OK

Si modération ON :
Images ajoutées seules puis copies ajoutées lorsque image validées status = 1 OK

Si modération passe de ON vers OFF :
Ajout de copies pour toutes les images non-approuvées.

Si modération passe de OFF vers ON :
Suppression des copies pour toutes les images non-approuvées

Si image change pour status = 1
Ajout de copies

Si image change pour status !=1
Suppression des copies

Si ajout d'image avec Moderation ON :
Pas de copies créées

Si ajout d'image avec modération OFF
Copies crées lors de l'ajout. status = 1 OK





On récupère au max X images, qu'on affiche dans l'ordre d'envoi,
On garde en mémoire les dernières images envoyées.
Une fois qu'on arrive à la fin du diaporama, on récupère les x nouvelles images. 
Si nombre d'images inférieur à X images, on ajoute les images gardées en mémoire à la fin du diaporama.
Si nombre d'images supérieur à X images, on garde les images affichées en mémoire.
Si pas de nouvelles images, on remet les images gardées en mémoire dans le diaporama.
Faire un reload quand on arrive à la fin des images affichées pour la première fois, si pas de nouvelles images, on continue le diaporama


1 2 3 4, reloard : +4

5 6 7 8 reload : null, 1 2 3 4 reload : + 4
9 10 11 12 reload : null, 5 6 7 8 1 2 3 4




UPDATES LARAVEL :
Updated Daisy UI, then removed Daisy UI line from tailwind config js,
    then added @plugin "daisyui"; to app.css
    then update tailwind with npx @tailwindcss/upgrade --force 

    Then update Mary UI : composer require robsontenorio/mary:^2.0

    then updated Laravel (laravel 12 in composer.json) then composer update

    remove dependancies :
        npm remove autoprefixer postcss
        npm remove autoprefixer postcss
        npm add --dev daisyui tailwindcss @tailwindcss/vite laravel-vite-plugin vite

    removed : postcss.config.js