6. Utilisation des Relations en Eloquent

Tu peux maintenant utiliser tes relations ainsi :
🔹 Récupérer les images d’un utilisateur

$user = User::find(1);
$images = $user->images; // Liste des images de l'utilisateur

🔹 Récupérer les catégories d’un utilisateur

$user = User::find(1);
$categories = $user->categories; // Liste des catégories créées par l'utilisateur

🔹 Récupérer toutes les images d’une catégorie

$category = Category::find(1);
$images = $category->images; // Liste des images de cette catégorie

🔹 Récupérer l’utilisateur et la catégorie d’une image

$image = Image::find(10);
$user = $image->user; // L'utilisateur qui possède cette image
$category = $image->category; // La catégorie de cette image


TO DEPLOY :
Added GITIGNORE, and cleaned git
Changed image thumbnail creation, use InterventionImage without new manager instance
Added possibility to set all four margins separatly (top, bottom, left, right)
Keaboard shortcuts (for deleteSelected) on moderation components
Keyboard shortcuts for modals (Cancel, confirm) on moderation parent/wrapper
Updated the deleteImage(s) functionalities on moderation's "all-images" component, so it updates status to 5 instead of instant delete (it was forgoten on this componenent)
Updated the "Images copies creation", "permanent = 0" code on setup-wall component, so it doesn't count the status=5 images when it calculates the number of copies to create.
Changed the tree organisation in livewire wall folder. Now Moderation and Display components are in different folders.
Changed display image component, now slideshow-wrapper haddles the slideshow container, and slideshow the slideshow and images.
Changed the propelled image research, so it's now working separatly with the "new image" research. 
    Now possible to change the number of "new image research" and keep a high rate of "propel image research".
Added transition 1ms to avoid blip/flick when changing slides.
Changed the copies deletion in markImageAsDisplay : status update instead of delete
Changed slideshow images update from database system :
    We check if there's a difference between the used list by the slidehow, and the database, if yes, re render.
    When we reach the end of the slideshow, we start again with the complete list
Added a cleanDeletedImages / Empty the bin function to the moderation component (deletes images with status = 5)


NEXT TO DO :
Bug sur display-image lors de l'archivage ou Suppression d'image.
    Lié à la Suppression des copies lors de l'archivage d'une image.
    Solution trouvée : Changer le status '5' puis supprimer la copie ulterieurement.
Trouver une solution pour supprimer les copies d'images avec status = 5 à un moment
Name and mail in create-image
Possibility to remove caption from an image
Add download button for QR Code
Smaller Image Preview - create images
Add possibility to display QR code on display-image
Full sreen zoom in Moderation needs change
Polices personnalisées
Personnalisation page "create-image"
Video as background on wall display
Options de transition dans slideshow
Définir la fréqence d'actualisation du slideshow par rapport à la bdd

MISE A JOUR LARAVEL : 
Modifier composer.json
puis Terminal : composer update


/*\ COMMAND LINES /*\
Reset BDD : 
php artisan migrate:fresh --seed

Start Vite server : npm run dev (or build)

Create volt component :
php artisan make:volt walls/display-images-dev --class



/*\ IMAGES COPIES LOGIC /*\
Si modération OFF :
Images ajoutées avec leurs copies, status = 1 OK

Si modération ON :
Images ajoutées seules puis copies ajoutées lorsque image validées status = 1 OK

Si modération passe de ON vers OFF :
Ajout de copies pour toutes les images non-approuvées.

Si modération passe de OFF vers ON :
Suppression des copies pour toutes les images non-approuvées

Si image change pour status = 1
Ajout de copies

Si image change pour status !=1
Suppression des copies

Si ajout d'image avec Moderation ON :
Pas de copies créées

Si ajout d'image avec modération OFF
Copies crées lors de l'ajout. status = 1 OK